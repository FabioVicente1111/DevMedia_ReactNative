import { StyleSheet } from 'react-native';

const styles = StyleSheet.create({
  container: {
    flex: 1,
    alignItems: 'center',
    padding: 20,
  },
  titulo:{
    textAlign: 'center',
    fontSize: 22,
    fontWeight: 'bold',
    marginBottom: 10,
  },
  texto:{
    fontSize: 16,
    textAlign: 'center',
    marginHorizontal: 15,
  },
});

export default styles;
